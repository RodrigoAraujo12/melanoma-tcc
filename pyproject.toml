[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "melanoma-tcc"
version = "0.1.0"

[tool.setuptools.packages.find]
where = ["."]
include = ["melanoma_tcc*"]
